``kubectl apply -f koan-deployment.yaml # To deploy the Koan pods in the default namespace. ``

``kubectl apply -f koan-service.yaml # To deploy a service that can route traffic to the Koan deployment. ``

